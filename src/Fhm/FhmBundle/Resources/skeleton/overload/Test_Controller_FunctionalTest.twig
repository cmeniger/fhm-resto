<?php

namespace {{ namespace }}\Tests\Controller;

{% block use_statements %}
use Symfony\Bundle\FrameworkBundle\Test\WebTestCase;
{% endblock use_statements %}

{% block class_definition %}
/*
Functional tests check the integration of the different layers of an application (from the routing to the views). They are no different from unit tests as far as PHPUnit is concerned, but they have a very specific workflow:
- Make a request
- Test the response
- Click on a link or submit a form
- Test the response
- Rinse and repeat
*/
class FunctionalTest extends WebTestCase
{% endblock class_definition %}
{
{% block class_body %}
    public function testShow()
    {
        $client  = static::createClient();
        $crawler = $client->request('GET', '/{{ document|lower }}');
        $this->assertGreaterThan(
            0,
            $crawler->filter('body:contains("{{ document|lower }}")')->count()
        );
    }
{% endblock class_body %}
}