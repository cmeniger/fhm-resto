{% trans_default_domain instance.domain %}
{% extends "::FhmFhm/Admin/detail.html.twig" %}

{% block content_data %}
    <div class="row">
        <h2>{{ (instance.translation ~ '.admin.detail.link')|trans }}</h2>
    </div>
    <div class="row">
        {#{{ macroTree.admin(tree, treemap, document, instance.translation ~ '.admin.detail.menu', instance) }}#}
    </div>
    {{ parent() }}
{% endblock %}
