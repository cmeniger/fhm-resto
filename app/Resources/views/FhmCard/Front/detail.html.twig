{% trans_default_domain instance.domain %}
{% extends "::FhmFhm/Front/detail.html.twig" %}

{% block content_link %}{% endblock %}

{% block content_data %}
    <div class="row hidden-for-small">
        <div class="column">
            {{ render(path('fhm_api_card_embed', {id:object.getId(), template:'tabs'})) }}
        </div>
    </div>
    <div class="row hidden-for-medium-up">
        <div class="column">
            {{ render(path('fhm_api_card_embed', {id:object.getId(), template:'accordion'})) }}
        </div>
    </div>
{% endblock %}