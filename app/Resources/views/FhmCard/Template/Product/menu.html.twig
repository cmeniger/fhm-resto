{% trans_default_domain instance.domain %}

{% set forward = false %}
{% if products %}
    <div class="product">
        <ul>
            {% for product in products %}
                {% if product.getForward() != forward %}
                    </ul><ul class="{{ product.getForward() ? 'forward small-block-grid-4' : '' }}">
                    {% set forward = product.getForward() %}
                {% endif %}
                {% include product.getForward() ? "::FhmCard/Template/Product/menu.forward.html.twig" : "::FhmCard/Template/Product/menu.inline.html.twig" %}
            {% endfor %}
        </ul>
    </div>
{% endif %}