{% trans_default_domain instance.domain %}

{% if demo is defined %}
    <nav class="nav-right">
        <ul>
            <li>
                <a href="#anchor-testimony">Témoignages</a>
            </li>
            <li>
                <a href="#anchor-contact">Nous contacter</a>
            </li>
        </ul>
    </nav>
{% else %}
    <nav class="nav-right">
        <ul>
            {% for branch in tree.branchs %}
                <li>
                    <a href="{{ branch.getRoute() }}">{{ branch.getName() }}</a>
                </li>
            {% endfor %}
        </ul>
    </nav>
{% endif %}